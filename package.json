{
  "name": "flashcard-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix api\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix client && npm install --prefix api"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
